--- "a"
+++ "b"
@@ -82,10 +82,6 @@
     }
 
     void dump() {
-
-        apply([] (i64 k, RegChange &e) {
-            fprintf(stderr, "%lld: %s = %d\n", k, RegEnum::key(e.reg), e.value);
-        });
     }
 };
 
